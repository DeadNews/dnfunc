[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.poetry]
name = "vendor"
version = "0.0.0"
description = "Vendor packages that are distributed as a single source file."
authors = ["Various Authors"]

[tool.poetry.dependencies]
python = "^3.10"
